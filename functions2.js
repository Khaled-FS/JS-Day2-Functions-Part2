function task(number) {
  console.log(`-----task ${number}------`);
}

function isOdd(n) {
  if (n % 2 == 0) {
    return false;
  } else {
    return true;
  }
}

task(1);
let result = isOdd(5);
console.log(result);

function squareOrDouble(n) {
  if (n % 2 == 0) {
    let numE = n;
  } else {
    return true;
  }
}
